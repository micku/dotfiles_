# install brew
$ ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

# check install
$ brew doctor

# install cask
$ brew install caskroom/cask/brew-cask

#  install latest zsh with homebrew 
$ brew install zsh

# get oh-my-zsh
$ curl -L http://install.ohmyz.sh | sh

# get the latest 
brew install vim

# first, install it!
$ brew install tmux
